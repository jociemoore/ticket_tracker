<tr>
  <td>
    <h2><%= link_to project.name, project_path(project.id) %></h2>
    <p><%= project.description %></p>
  </td>
  <td><%= project.total_open_tickets %></td>
  <td><%= project.tickets.size %></td>
</tr>
